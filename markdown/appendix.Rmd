# Included data
(ref:table-db-overview) Publications included in TED
```{r table-db-overview}
apa_table(references_table %>% filter(publication_id %in% unique(analysis_data$publication_id)),
          caption = "Publications included in TED")
```
